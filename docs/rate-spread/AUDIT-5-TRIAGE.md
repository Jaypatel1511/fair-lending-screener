# Audit-5 Triage — planning-chat disposition (the decisive round)

**Verdict: CONCUR — NO-GO, both kinds.** Defer the rate-channel pricing feature; do NOT write a v6. Separately and urgently: **CRIT-1 is a live false claim in the shipped v0.2.1 package and needs a 0.2.2 correction independent of v0.3.0.** I verified both the false-claim locations (repo grep) and the FFIEC contradiction (web) myself.

## The two outcomes

### 1. CRIT-1 — live, urgent, independent of pricing. Fix now.
The package ships, in v0.2.1 on PyPI, the claim that this is "the methodology OCC, Federal Reserve, FDIC, NCUA, and CFPB examiners use" / "the canonical reference used by [5 agencies]." **Verified present** in: `pyproject.toml:8` (PyPI description), `__init__.py:4`, `README.md:8` + `:202`, `docs/methodology.md:28`, `fair_lending_screener/_methodology_doc.md:28` (in the wheel), `report.py:244` (generated in output), and the demo notebook.

**The cited source contradicts it** (FFIEC Interagency Fair Lending Examination Procedures, Aug 2009 — real, current). Verified: "Examiners should **not** follow Table B when conducting a pricing review that involves a regression analysis... consult with agency supervisory staff" (corroborated via web). The auditor further quotes, from the primary doc, that the Procedures route HMDA-only reviews to comparative file review (not regression) and that "these procedures do not call for examiners to plan examinations to identify or focus on potential disparate impact issues" — while the package is branded "disparate impact analysis."

**Also:** the 2009 title page lists OTS (abolished 2011), not CFPB (didn't exist in 2009) — the 5-agency list is anachronistic both ways. And an adjusted protected-class coefficient with controls is a **disparate-treatment** screen, not disparate-impact — a second label error `[the treatment-vs-impact point I have NOT independently verified; the "examiners use" falsity I have]`.

**Disposition:** strike the "methodology examiners use" / "canonical reference" provenance claim at every location and ship **0.2.2**. Honest replacement, FFIEC-verified: the tool applies **FFIEC-standard controls** (the borrower/loan characteristics examiners consider) to public HMDA and is **informed by** the FFIEC risk-factor framework (rate spread is scoping risk factor P6) — it is **not** "the methodology examiners use," and examiners work from full loan files and route statistical analysis to specialized agency procedures. This is a doc/string correction, no analytical change — narrow, but the replacement wording gets verified against FFIEC before ship (diligence, not a fresh 5-round audit).

**My miss, owned:** I graded this "unsourced, not false" in v4 and deferred it to Jay as a marketing nit across two rounds. It is false and published. That was a misjudgment — a live false regulatory claim should have been pulled forward as a correction the moment it surfaced, not parked behind the pricing work.

### 2. Rate-channel pricing feature — DEFER. Blocked on data, not drafting.
The audit's "not worth shipping" ruling is correct, and the reason is not hedging — it is **CRIT-2: the tool has no credit-risk control at all**, while credit score is mechanically in the mortgage rate via GSE LLPA pass-through. β_pc is a sum of ≥4 channels (LLPA credit-risk pass-through — omitted, known-positive, unbounded; between-lender sorting; within-MSA geographic sorting; possibly discrimination), labeled "rate-channel disparity," with no way to tell the user the mix. Cross-market prioritization fails because ranking by β_pc ranks by local racial credit-score disparity and lender mix, not conduct.

**My miss, owned:** §9's claim that Table 10 "shares the tool's credit-score handicap" was false and I got it backwards. Bartlett does **not** share it — he **proxies** for it (tract × loan-type × minority-status credit averages from proprietary McDash/Equifax) and states plainly that observing the credit axis is what makes identification possible. The tool conditions on LTV (one axis of the 8×8 GSE grid) and omits credit score (the other). By the logic of the very paper I leaned on, β_pc can and will reflect differential credit risk.

**Why this is defer, not fix:** a first-pass search finds no public tract-level credit-score dataset; Bartlett's proxy needed proprietary data. So the feature is blocked on **data availability**, which is an empirical question to answer BEFORE any v6 — not something a sixth prose revision resolves. The audit is right: "a sixth revision that polishes §9's prose while the equation still omits the LLPA credit axis will fail for the fifth time in the same place."

## What the five rounds actually bought (the process worked)
No code was ever written. Five methodology rounds + hostile audits concluded that an adjusted rate-channel disparity **cannot be estimated defensibly on public HMDA** because the single largest legitimate rate driver (credit score) is absent and unproxiable with public data. That is methodology-first delivering its whole point: we spent doc-and-audit effort, not shipped-code effort, and did not publish a tool making refutable disparity claims under a false examiner banner. And the process surfaced a **live** false claim (CRIT-1) that predates this work. Both are wins, not five wasted rounds.

## Findings I accept without needing to re-litigate (verified by the auditor from primary text)
CRIT-3 (lender FE is a nuisance control, not an output — omitting it was a category error; adding it + reporting both coefficients would decompose the sorting channel and names no one); HIGH-1 (market-level silently excludes small banks/CUs via the (a)(12) optional-data exemption — backwards for an advocacy tool); HIGH-2/HIGH-3 (reason-code vocabulary incomplete + Exempt≠NA violated in 4 fields → identity can't hold; the signature failure); HIGH-4 (per-LEI is_arm misclassifies all-fixed lenders and has an undefined branch that defaults to a fabricated is_arm=0); HIGH-5/HIGH-6 (G_eff misattributed to CSS — it's Potthoff inverse-Herfindahl and ignores the design; Oster δ needs an R_max the doc never specifies); the MED/LOW citation defects. **All real; all moot for now because the feature defers — but they carry forward if pricing is ever revived, and HIGH-2/HIGH-3 (the reason-code/Exempt-NA failures) are the portfolio signature failure recurring, worth a durable note.**

## Statistics-reference lesson (new durable gotcha)
The [SET IN AUDIT-5] statistics citations I adopted from Audit-4 (CSS effective clusters, Oster δ, Webb, BKW) were each real papers **misattributed or misapplied** — G_eff isn't CSS's formula, Oster's δ needs R_max, BKW's κ forbids the partialling the doc proposed, Webb's few-cluster switch is actually Roodman et al. Same class as the version-discipline and 1111 gotchas: **a statistics method/threshold is unverified until the primary paper is read for what it actually computes and requires — not adopted from a prior chat's citation.** (Adding to working principles.)

## Decisions for Jay
1. **0.2.2 correction — proceed now?** (My strong rec: yes. Live false regulatory claim on PyPI.) If yes, I'll draft the exact per-location edits + CHANGELOG + a one-line release runbook (you execute git/tag/publish; a light FFIEC-wording check, not a 5-round audit).
2. **Pricing feature — confirm defer, stop the methodology rounds?** (My rec: yes; the gate is the credit-data question, answered separately before any revival, not in a v6.)
